{
  "name": "opencortex-memory",
  "version": "0.5.0",
  "description": "OpenCortex Memory Server — AI agent memory with reinforcement learning and multi-tenant isolation.",
  "author": "StardustVision",
  "mcpServers": {
    "opencortex": {
      "command": "node",
      "args": ["${extensionPath}${/}lib${/}mcp-server.mjs"],
      "cwd": "${extensionPath}"
    }
  }
}
